#!/usr/bin/env python

def drawGrid(frame, corners):
	"""
	Draws a grid corresponding to the rectangluar planar surface denoted by the
	list of corners as (x, y) coordinates in the frame (unwarped). Returns the
	frame (though there is no promise of not modifying the frame passed in).
	"""

	pass